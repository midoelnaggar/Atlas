import * as React from "react";
import { Center, Text } from 'native-base';

export default () => {
    return(
        <Center flex={1} >
                <Text color="#0BE082" fontSize="4xl">
                    بحث
                </Text>
        </Center>
    );
}